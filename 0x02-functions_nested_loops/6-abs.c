#include "main.h"
/**
 * _abs -Computes is a function that computes the absolute value of an integer
 * @a: an integer that the function tries to compute
 * Return: the computation of the integer
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
