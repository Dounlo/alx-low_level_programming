#include "main.h"
/**
 * add - adds two integers
 * @a: the first integer
 * @b: the second integer to add to the first one
 * Return: an integer that is the result
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
