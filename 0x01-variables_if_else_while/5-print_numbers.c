#include <stdio.h>
/**
 * main -Edit point
 *
 * This a program to write number
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int h;

	h = 0;

	for (h = 0; h < 10; h++)
	{
		printf("%d", h);
	}
	printf("\n");
	return (0);
}
