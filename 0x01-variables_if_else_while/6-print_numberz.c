#include <stdio.h>
/**
 * main -Edit point
 *
 * This a program that prints
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;
	char nl;

	a = 0;
	nl = '\n';

	for (a = 0; a < 10; a++)
	{
		putchar(a);
	}
	putchar(nl);
	return (0);
}

